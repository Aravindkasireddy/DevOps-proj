apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fin-enterprise-staging

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml

commonLabels:
  environment: staging

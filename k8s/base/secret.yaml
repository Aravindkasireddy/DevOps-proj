apiVersion: v1
kind: Secret
metadata:
  name: fin-enterprise-api-secrets
type: Opaque
stringData:
  # Replace via External Secrets Operator or Sealed Secrets in real deploy
  DATABASE_URL: postgresql+asyncpg://finenterprise:CHANGEME@postgres.fin-enterprise.svc:5432/fin_enterprise_assets
